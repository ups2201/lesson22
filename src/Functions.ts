import {Operation} from "./Operation";
import {Category} from "./Category";

function getOutcomeCashCategoryForPeriod(categoryName: string, startDate: Date, endDate: Date): Array<Category> {
    return [];
}

function getOperationForPeriod(startDate: Date, endDate: Date): Array<Operation> {
    return [];
}

function getSumForOperation(operationList: Array<Operation>): number {
    return 0;
}

function sortCategoryForPeriod(startDate: Date, endDate: Date): Array<Category> {
    return [];
}
